/* exported last */

// will create a function that returns the last element of an array

// returns the array element that is indexed at the length of the array minus one

function last(array) {
  return array[array.length - 1];
}
