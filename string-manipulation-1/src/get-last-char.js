/* exported getLastChar */

// will create a function that takes in a String
// by getting the length the the string and subtracting by 1
// it will allow the user to get the very last character of the string.
// Once the last character is retrieved, will return the last character

function getLastChar(string) {
  return string[string.length - 1];
}
