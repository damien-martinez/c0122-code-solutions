/* exported getFirstChar */

// created a function that passes in a string and takes a
// look at the very first character of the string and returns it

function getFirstChar(string) {
  return string[0];
}
