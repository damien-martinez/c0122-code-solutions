/* exported setValue */

/* a function that has 3 parameters, an Object, a Key, and a value and adds
the keya nd value to the object */

// create a new property and value for object parameter

function setValue(object, key, value) {
  object[key] = value;
}
