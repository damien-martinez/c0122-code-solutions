/* exported getValue */

/* function that takes in 2 parameters, an object and a key, and
returns the value of that key */

// returns the object at that specified key

function getValue(object, key) {
  return object[key];
}
